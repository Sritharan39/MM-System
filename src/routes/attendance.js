const express = require('express');
const router = express.Router();

// Placeholder - will be updated in Phase 4

router.get('/', (req, res) => {
  res.json({ message: 'Attendance endpoint - Phase 4' });
});

module.exports = router;
