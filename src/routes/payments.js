const express = require('express');
const router = express.Router();

// Placeholder - will be updated in Phase 3

router.get('/', (req, res) => {
  res.json({ message: 'Payments endpoint - Phase 3' });
});

module.exports = router;
