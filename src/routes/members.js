const express = require('express');
const router = express.Router();

// Placeholder - will be updated in Phase 2

router.get('/', (req, res) => {
  res.json({ message: 'Members endpoint - Phase 2' });
});

module.exports = router;
